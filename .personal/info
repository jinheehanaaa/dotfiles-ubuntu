### Initial Setup After OS INSTALL ###
https://www.youtube.com/watch?v=5b-xQkRWHjQ&t=879s


# INSTALL DRACULA FOR GEDIT
wget https://raw.githubusercontent.com/dracula/gedit/master/dracula.xml
mkcd /home/jinheehan/.local/share/gedit/styles/
mv dracula.xml $HOME/.local/share/gedit/styles/

# INSTALL DRACULA FOR gtok


##### System #####
lspci -v -s $(lspci | grep VGA | cut -d" " -f 1)
sudo nano /etc/default/grub
sudo apt remove gnome-shell-extension-ubuntu-dock

##### DOCKER SETUP #####
sudo snap install docker
sudo addgroup docker
sudo adduser $USER docker
boot
docker run hello-world

##### PERMISSION #####
chmod +x test.zsh

##### GIT #####
git init
git clone https://github.com/rbenv/rbenv.wiki.git ~/.rbenv
git status
git add .
git log
git commit -am "Add setup_node.zsh and matching Dotbot command"
git push
